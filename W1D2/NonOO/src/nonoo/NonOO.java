/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package nonoo;

/**
 *
 * @author mzijlstra
 */
public class NonOO {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        // rotate()
        // playSound();
    }
    
    public static void rotate(Object shape, int typeOfShape) {
        if (typeOfShape == 1) { // circles
            // rotate a circle code
            // looking at the shape data
        } else if (typeOfShape == 2) { // squares
            // rotate a square 
            // looking at the shape data
        } else if (typeOfShape == 3) { // triangle
            // rotate a triangle
            // by looking at the shape data
        } else if (typeOfShape == 4) { // Amoeba
            // rotate the amoeba
            // by looking at the shape data
        }
    }
    
    public static void playSound(Object shape, int typeOfShape) {
        if (typeOfShape == 1) { // circles
            // circle code to play sound
            // looking at the shape data
        } else if (typeOfShape == 2 || typeOfShape == 3) { // squares
            // square code to play sound
            // looking at the shape data
        } else if (typeOfShape == 4) { // Amoeba
            // amoeba code 
            // by looking at the shape data
        }
    }
    

}
